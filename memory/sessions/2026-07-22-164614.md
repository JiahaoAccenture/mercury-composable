# Session (2026-07-22T16:46:14.546Z)

**Agent:** Codex

Implemented a repository-shared, bounded `ui-loop-engineer` workflow for one-prompt software delivery.

What changed:
- Added `agent-skills/ui-loop-engineer/SKILL.md` as the default end-to-end implementation workflow:
  frame requirement, discover, specify, implement in slices, verify, run fresh-context review, fix,
  and stop only at measurable Definition of Done or a human gate.
- Adopted the user's personal spec checklist into the repository as
  `agent-skills/loop-spec-plan/SKILL.md`. The distinct name avoids duplicate `spec-plan` selectors;
  the personal `~/.codex/skills/spec-plan` remains unchanged.
- Added controlled autonomous planning. Corrective checklist failures receive one bounded retry;
  material ambiguity, durable architecture decisions, unsafe actions, unavailable required evidence,
  and exhausted review budgets remain human gates.
- Added a mandatory durable-design classification before implementation, strict verification
  equivalence, and a global non-resettable three-round review/fix budget.
- Updated `AGENTS.md` so implementation-authorizing requests use `ui-loop-engineer` by default while
  analysis-only, planning-only, review-only, explanation-only, and Q&A requests remain read-only.
- Regenerated 54 local vendor adapters for 9 skills; adapters are gitignored and are not committed.

Verification:
- Official `quick_validate.py` passed for both new skills (PyYAML installed only under
  `/private/tmp/codex-skill-validator-deps`).
- `git diff --check` passed; no TODO, legacy `$ARGUMENTS`, or stale `spec-plan` path remained.
- Three fresh-context read-only subagents reviewed workflow consistency, portability, and safety.
  Their supported findings were fixed, and all three targeted rechecks reported the findings resolved.
- A separate `codex exec` forward-test could not run because the user's global npm Codex wrapper is
  missing its native Darwin ARM64 binary (`ENOENT`); this did not affect the in-app subagent review.

Also completed the first `ui-loop-engineer` product run for the graph export saved-state bug:
- Confirmed `graph.exported` now drives both the header state and local bookmark for console and
  button exports; both paths display `Saved: <name>` plus the matching `Load Graph` entry.
- Split the remembered export name from the clean/dirty indicator, so graph mutations return the
  button to `Save Graph` while preserving the next save-form default.
- Added WebSocket `connectionEpoch` tracking so save-name state survives same-session SPA navigation
  but cannot leak across disconnect/reconnect cycles, including off-route reconnects.
- Added accessible focus restoration, stronger saved-state contrast, long-name truncation, reducer /
  protocol / rendered-markup tests, and synchronized technical documentation.
- Verification: 147 Vitest tests and the Vite production build passed; `tsc --noEmit` remains blocked
  only by the pre-existing `useGraphAuthoring.ts:424-426` union-narrowing errors. Three bounded
  fresh-context review rounds completed and all supported findings were addressed. A targeted
  follow-up review found no issue in the mounted export workflow; cross-route in-flight export
  persistence remains an existing out-of-scope lifecycle boundary.

## Memory References
- Referenced: [[vision-mercury-composable]]
